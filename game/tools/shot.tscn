; Обвязка для съёмки: пустой узел со скриптом, который подгружает нужную
; сцену сам. Отдельная сцена, а не правка главной, чтобы съёмка не зависела
; от того, что сейчас стоит главной сценой проекта.

[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://tools/shot.gd" id="1_shot"]

[node name="Shot" type="Node3D"]
script = ExtResource("1_shot")
